import React from 'react';

const Footer = () => {
    return (
        <div className="footerdesign">
            {/* Anything we want to put in the footer */}
        </div>
    );
};

export default Footer;